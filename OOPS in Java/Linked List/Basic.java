// What is LinkedList in Java?

 //       > LinkedList is a class in Java Collection Framework that is used to store elements in a linear order but not in a fixed-size array — instead, every element (called a node) is linked to the next.